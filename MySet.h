#pragma once

//include proper header files

template<typename ItemType>
class MySet
{
public:

   // default constructor, creates empty list

   // copy constructor: if deep copy is needed

   // assignment operator if you need to provide copy constructor

   // destructor if copy constructor is provided

   // insert a data item to the set. No duplicate items allowed in the list

   // remove a given value from the set

   // check if a given value is in the set or not

   // get the number of items in the set

   // Remove all items from the set

private:

   //pick a data structure to hold items in the set. The data structure
	//can be list, vector, array, or others you prefer
};

#include "MySet.cpp"	//need to inlcude the .cpp file of the class template

